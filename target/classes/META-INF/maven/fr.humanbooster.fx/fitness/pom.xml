<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<!--La balise groupId correspond à l'entreprise où l'on travaille -->
	<groupId>fr.humanbooster.fx</groupId>
	<!-- La balise artifactId correspond au nom du projet -->
	<artifactId>fitness</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<!-- La balise packaging contient le type de fichier que Maven doit produire -->
	<packaging>jar</packaging>

	<!-- La balise name indique le nom du projet tel qu'il apparait dans l'IDE -->
	<name>fitness</name>
	<!-- La balise url indique l'adresse où l'application a été déployée -->
	<url>http://maven.apache.org</url>

	<!-- La balise properties regroupe l'encodage et les versions des bibliothèques 
		et -->
	<!-- framework -->
	<!-- La balise properties permet de définir des variables qui seront utilisables -->
	<!-- partout dans le fichier pom.xml -->
	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
	</properties>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.8.1</version>
				<configuration>
					<source>17</source>
					<target>17</target>
				</configuration>
				<executions>
					<execution>
						<phase>compile</phase>
						<goals>
							<goal>compile</goal>
						</goals>
					</execution>
				</executions>
				<dependencies />
			</plugin>
		</plugins>
	</build>
	
	<!-- La balise dependencies inclut toute les dépendances du projet -->
	<dependencies>
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<version>3.8.1</version>
			<scope>test</scope>
		</dependency>
		<!-- https://mvnrepository.com/artifact/org.apache.poi/poi -->
<dependency>
    <groupId>org.apache.poi</groupId>
    <artifactId>poi</artifactId>
    <version>5.2.3</version>
</dependency>

<!-- https://mvnrepository.com/artifact/org.projectlombok/lombok -->
<dependency>
    <groupId>org.projectlombok</groupId>
    <artifactId>lombok</artifactId>
    <version>1.18.28</version>
    <scope>provided</scope>
</dependency>

	</dependencies>
</project>
